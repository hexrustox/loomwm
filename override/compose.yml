services:
  dev:
    volumes:
      - ${XDG_RUNTIME_DIR}/${WAYLAND_DISPLAY}:${XDG_RUNTIME_DIR}/${WAYLAND_DISPLAY}:ro
    environment:
      - XDG_RUNTIME_DIR
      - WAYLAND_DISPLAY
    devices:
      - /dev/dri
